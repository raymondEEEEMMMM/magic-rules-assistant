# 虚拟环境设置指南

## ✅ 虚拟环境已创建

项目的 Python 虚拟环境已成功创建并配置完成。

---

## 📋 虚拟环境信息

- **位置**: `/Users/lianghaoming/cbworkplace/venv/`
- **Python 版本**: 3.9.6
- **创建时间**: 2026-03-13

---

## 🚀 使用虚拟环境

### 激活虚拟环境

```bash
# 在项目根目录下执行
source venv/bin/activate
```

激活后，命令提示符会显示 `(venv)` 前缀。

### 验证虚拟环境

```bash
# 查看 Python 路径
which python
# 应显示: /Users/lianghaoming/cbworkplace/venv/bin/python

# 查看已安装的包
pip list
```

---

## 📦 已安装的依赖

虚拟环境中已安装所有必需的依赖包：

| 包名 | 版本 | 用途 |
|------|------|------|
| fastapi | 0.104.1 | Web 框架 |
| uvicorn | 0.24.0 | ASGI 服务器 |
| pydantic | 2.5.0 | 数据验证 |
| python-dotenv | 1.0.0 | 环境变量管理 |
| openai | 1.3.0 | OpenAI API |
| requests | 2.31.0 | HTTP 请求 |
| beautifulsoup4 | 4.12.2 | HTML 解析 |
| apscheduler | 3.10.4 | 定时任务 |

---

## 🔄 日常操作

### 安装新的依赖

```bash
# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install <package-name>

# 更新 requirements.txt
pip freeze > requirements.txt
```

### 升级依赖

```bash
# 升级单个包
pip install --upgrade <package-name>

# 升级所有包
pip install --upgrade -r requirements.txt
```

### 退出虚拟环境

```bash
deactivate
```

---

## 🛠️ 故障排查

### 问题：虚拟环境无法激活

**错误信息**: `venv/bin/activate: No such file or directory`

**解决方案**:
```bash
# 重新创建虚拟环境
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 问题：依赖包未安装

**错误信息**: `ModuleNotFoundError: No module named 'fastapi'`

**解决方案**:
```bash
# 确保虚拟环境已激活
source venv/bin/activate

# 重新安装依赖
pip install -r requirements.txt
```

### 问题：Python 版本不匹配

**解决方案**:
```bash
# 检查系统 Python 版本
python3 --version

# 使用正确的 Python 版本创建虚拟环境
python3 -m venv venv
```

---

## 📝 最佳实践

1. **始终使用虚拟环境**
   - 避免污染系统 Python 环境
   - 隔离项目依赖

2. **激活后再操作**
   - 运行脚本、安装包前先激活虚拟环境

3. **及时更新 requirements.txt**
   - 每次安装新包后更新依赖列表
   - `pip freeze > requirements.txt`

4. **使用 .gitignore**
   - 虚拟环境目录已被忽略
   - 不要提交 venv 目录到版本控制

---

## 🎯 快速命令参考

```bash
# 激活虚拟环境
source venv/bin/activate

# 退出虚拟环境
deactivate

# 查看已安装的包
pip list

# 安装依赖
pip install -r requirements.txt

# 启动服务（使用启动脚本，自动激活虚拟环境）
./start.sh

# 运行测试
python test_first_stage.py
python test_card_service.py
```

---

**✨ 虚拟环境配置完成，可以开始开发了！**
